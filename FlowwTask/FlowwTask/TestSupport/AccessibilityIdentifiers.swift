import Foundation

enum AccessibilityIdentifiers {

    enum Components {
        enum Generic {
            static let textField = "TextField"
            static let label = "Label"
        }
    }
}
